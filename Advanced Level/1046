#include<cstdio>
#include<algorithm>
using namespace std;

const int maxn = 10010;
int A[maxn], dis[maxn];
 
 int main() {
 	int n, k, sum, l, r, m;
 	scanf("%d", &n);
 	for(int i = 1; i <= n; i++) {
 		scanf("%d", &A[i]);
 		sum += A[i];
 		dis[i] = sum;
	 }
	 scanf("%d", &m);
	 for(int i = 1; i <= m;i++) {
	 	scanf("%d%d", &l, &r);
	 	if(l > r) {
	 		swap(l, r);
		 }
	 	int temp = dis[r - 1] - dis[l - 1];
	 	printf("%d", min(temp, sum - temp));
	 }
 }
