#include<cstdio>
const int N = 54;
char sh[5] = {'S','H','C', 'D', 'J'};
int start[N + 1];
int end[N + 1];
int next[N + 1];

int main() {
  int n;
  scanf("%d", &n);
  for(int i = 1; i <= N; i++) {
	start[i] = i;
  }
  for(int i = 1; i <= N; i++) {
	scanf("%d", &next[i]);
  }
  for(int step = 0; step < n; step++) {
	for(int j = 1; j <= N; j++) {
		end[next[j]] = start[j];
	}
	for(int j = 1; j <= N; j++) {
		start[j] = end[j];
	}
  }
  for(int i = 1; i <= N; i++) {
	 if(i != 1) printf(" ");
         start[i]--;
         printf("%c%d", sh[start[i]/13], start[i] % 13 + 1);
  }
  return 0;
}
